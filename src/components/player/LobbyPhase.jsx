import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

const LobbyPhase = () => (
  <Card>
    <CardContent>
      <Typography>Waiting for game start…</Typography>
    </CardContent>
  </Card>
);

export default LobbyPhase;
